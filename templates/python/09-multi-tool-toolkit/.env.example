# Session state TTL in seconds (idle sessions are purged)
SESSION_TTL=3600

# Maximum context items per session (oldest evicted on overflow)
MAX_CONTEXT_ITEMS=100

# Directory where export_data writes files
EXPORT_DIR=./exports
